# Generated by the learner's own runs — never shipped in the handout. implementations/ in particular
# would carry an agent's earlier answer to the very exercise, which is the leak this build exists to avoid.
implementations/
normalisation.txt
.kotlin/
forks/*/
forks/generated-with.txt
