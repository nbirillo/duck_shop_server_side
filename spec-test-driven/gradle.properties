kotlin.code.style=official

# Which implementation `checkPrimary` runs against: "starter", "grading", or a solutions/<name>/.
primaryAgent=starter

# compareAgents runs the solution modules' tests in parallel.
org.gradle.parallel=true
