// A hand-seeded, deliberately-broken solution used to prove the shared acceptance suite
// actually catches a wrong implementation. Not a real agent — see agent.json.

plugins {
    id("duck-shop.solution")
}
